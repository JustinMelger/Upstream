[pytest]
markers =
    unit: Unit tests (services/config/db)
    integration: Integration tests (API endpoints)
    architecture: Architecture/structure contract tests
pythonpath = .
addopts = --import-mode=importlib
